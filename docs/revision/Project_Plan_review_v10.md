# Project_Plan 变更日志（v10）

## 变更日期

2026-05-15

## 变更动机

在 M1、M2 的核心内容已经并入 `docs/Project_Plan.md` 后，原 `docs/milestone/` 下的独立里程碑文档已经不再承担主文档之外的独立信息。为避免重复维护与文档分叉，本次删除原 `M1.md`、`M2.md`，并同步修正文档中对其“仍保留”的描述。

---

## 变更清单

### 1. `docs/milestone/` — 删除原里程碑文档

删除以下文件：

- `docs/milestone/M1.md`
- `docs/milestone/M2.md`

### 2. `docs/Project_Plan.md` — 同步更新引用与目录结构

| 位置 | 变更内容 |
| ---- | -------- |
| §1.4 当前实现同步 | 将“原独立里程碑文档仍作为归档保留”更新为“不再单独保留” |
| §3.1 当前真实目录结构 | 移除 `docs/milestone/` 子树示意 |
| Phase 1 当前同步结果 | 将对 `docs/milestone/M1.md` 的引用改为“阶段性记录已整合，不再单独保留里程碑文档” |
| Phase 2 当前同步结果 | 将对 `docs/milestone/M2.md` 的引用改为“阶段性记录已整合，不再单独保留里程碑文档” |

---

## 影响评估

| 影响项 | 说明 |
| ------ | ---- |
| 文档维护成本 | M1 / M2 信息源收敛到 `docs/Project_Plan.md`，避免主计划与独立里程碑双份维护 |
| 历史记录 | 里程碑内容仍保留在主计划文档对应 Phase 段落中，同时 revision 日志保留本次删除记录 |
| 代码与实验 | 本次仅删除冗余文档并更新文字说明，不涉及代码、测试或实验结果 |

---

## 未修改的文件

- `docs/revision/Project_Plan_review_v9.md`：保留上一轮“整合里程碑内容”的修订记录
- `src/`、`tests/`、`scripts/`：本次未做任何代码或测试改动
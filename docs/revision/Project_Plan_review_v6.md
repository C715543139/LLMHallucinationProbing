# Project_Plan 审查与修改建议（v6）

## 1. 审查结论

本轮 v6 为**对照课程要求的完整性核查**，同时确认 v5 遗留问题的修复状态，并做最终收口评估。

**总体结论：计划文档完整覆盖课程所有要求，无遗漏项，文档层面的审查在本轮结束。**

---

## 2. 课程要求覆盖情况（逐项核查）

| 课程条目                                 | 计划中覆盖情况 | 对应位置                                                  |
| ---------------------------------------- | -------------- | --------------------------------------------------------- |
| **3.1** 基于生成概率（PPL）方法实现      | ✅ 已覆盖      | P2.1–P2.2，含伪代码与验证集阈值调优协议                   |
| **3.1** 基于隐藏层分类（SAPLMA）方法实现 | ✅ 已覆盖      | P2.3–P2.5，含逻辑回归 + MLP 分类器选型                    |
| **3.1** 探索不同层特征质量               | ✅ 已覆盖      | P3.2，层深度-准确率曲线，含层编号约定                     |
| **3.2** PPL vs SAPLMA 对比并分析原因     | ✅ 已覆盖      | P3.4，含 4 个维度的对比表格与原因分析                     |
| **3.2** 不同 token 位置检测性能变化      | ✅ 已覆盖      | P3.3，First/Last/Mean pooling 保底，Subject token 可选    |
| **3.3①** 注意力模式分析 + 改进检测方法   | ✅ 已覆盖      | 方向 A，含动机、陈述句适配说明、消融实验                  |
| **3.3②** FFN 知识熵分析 + 改进检测方法   | ✅ 已覆盖      | 方向 B，含 Knowledge Entropy 特征与对比实验               |
| **3.3③** MoE/DLLM 特定架构分析（可选）   | ✅ 已覆盖      | 方向 C，列出 OLMoE-1B-7B 与 LLADA-8B-base                 |
| 提交：完整代码文件                       | ✅ 已覆盖      | 目录结构覆盖所有模块，PowerShell 运行脚本齐全             |
| 提交：PDF 报告含 3.1/3.2 详尽图表        | ✅ 已覆盖      | 图表清单 F1–F7，报告结构建议完整                          |
| 提交：3.3 需含动机、实现细节、对比实验   | ✅ 已覆盖      | 每个进阶方向均含动机说明、特征工程示例、消融/对比实验设计 |

---

## 3. v5 遗留问题修复确认

### 问题 ① P1.1 输出物统一为 pyproject.toml + uv.lock

**状态：✅ 已修复（在 v6 核查前已修复）**

当前文档中 P1.1、目录结构与复现约定均统一为 `pyproject.toml` 与 `uv.lock`，无 `environment.yml` 残留。

### 问题 ② 注意力特征示例与 P4.A2 任务描述对齐

**状态：✅ 已在本轮（v6）修复**

已在 `docs/Project_Plan.md` 的特征工程示例中补充 `relation_attn_ratio`（对关系词 token 的关注比例）和 `tail_attn_ratio`（对句尾 token 的关注比例），与 P4.A2 任务描述"主语实体 token、关系词 token 与句尾 token 之间的注意力统计特征"完全对齐。

修复后特征工程示例：

```python
attention_features = {
    "attn_concentration": entropy(attention_weights),       # 注意力集中度（熵越低越集中）
    "entity_attn_ratio": attn_to_entity / attn_total,       # 对主语实体的关注比例
    "relation_attn_ratio": attn_to_relation / attn_total,   # 对关系词 token 的关注比例
    "tail_attn_ratio": attn_to_tail / attn_total,           # 对句尾 token 的关注比例
    "cross_head_agreement": mean_head_cosine_similarity,    # 多头注意力一致性
    "attn_entropy_per_layer": [h_i for h_i in entropies],   # 逐层注意力熵
}
```

---

## 4. 整体评估

经过 v1–v6 共六轮迭代审查，计划文档已达到以下标准：

- **课程要求全覆盖**：3.1（简单任务）、3.2（分析任务）、3.3（进阶任务）三个部分的所有要求均有对应的实验设计、输出物定义和评估协议。
- **实验协议严谨**：数据划分、随机种子约定、训练集/验证集/测试集职责划分、统一评估指标均已明确。
- **工程可执行性强**：Windows PowerShell 脚本、uv 依赖管理、路径兼容性、显存预算分析均已具体化。
- **风险已识别并有对策**：bitsandbytes Windows 兼容性、Llama-2 访问权限、显存不足等主要风险均列出了具体对策。
- **文档内部一致**：环境配置文件（pyproject.toml + uv.lock）、注意力特征示例与任务描述等内部口径已统一。

**文档层面的审查到此收口。后续进入代码实现阶段。**
